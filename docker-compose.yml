version: '3.8'

services:
  benfoggon-site:
    build: .
    container_name: benfoggon-portfolio
    restart: unless-stopped
    ports:
      - "8082:80"
    networks:
      - web

networks:
  web:
    driver: bridge
